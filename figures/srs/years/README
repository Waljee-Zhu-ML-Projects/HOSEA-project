File name structure:

<Which prior years experiment>
_<Cancer Type>
_<Subset by missingness>
.<File type>

In particular:
- Experiment:
  - 2y: a sliding window of two years of data
- Cancer type: 
  - ANY, EAC, EGJAC determine which model is used 
  - subsetting the test set by removing cases that are inappropriate (e.g., EAC model removes EGJAC cases)
- Subset by missingness: 
  - all: everything is used
